# Required metadata
sonar.projectKey=nonogramm
sonar.projectName=Nonogramm
sonar.projectVersion=0.1

# Comma-separated paths to directories with sources (required)
sonar.sources=src/main/java

#sonar.binaries=target\classes
